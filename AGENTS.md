# 仓库工作约定

- 在 Git 的 `main` 分支上工作。旧的 `main-backup` 分支已经弃用并删除。
- Android 默认构建和安装目标为 `vienna`（`:app:assembleVienna`）。
- `vienna` 构建保留应用 ID `app.mihon.dev`，以便原位覆盖升级。
- 除非用户明确要求，否则不要构建或安装 `localFirst`、`release` 或其他变体。
- 对已连接的主设备，优先使用 `arm64-v8a` APK。

## 核心工程方向

- 增加新行为前，先保留现有优势和使用习惯。启动速度、缓存即时显示、滚动位置、阅读进度、本地身份、自定义封面和安静的后台更新都属于功能契约的一部分。
- 优先扩展距离需求最近的现有功能、状态流、仓库、缓存或界面入口。已有路径能够承载时，不要另建平行实现或第二份事实来源。
- 采用能够解决根因的最小完整改动。调整方向后，删除已经被取代的恢复分支、重复扫描、临时状态流，以及不再有明确用途的兼容代码。
- 不要在尚未理解状态模型时不断叠加防御补丁。先追踪从数据生产者到界面的完整链路，再修正共同的数据归属或失效边界。
- 性能下降也属于功能回归。评估改动时，要按用户真实的本地库规模考虑，其中可能包含 3000 部以上漫画和数千个篇目文件。

## 本地库行为

- 本地库必须立即显示最后一次确认正常的缓存或索引结果。普通进入页面和切换筛选时，不得等待文件系统重新扫描。
- 使用轻量元数据或签名检查发现新增、删除和移动，只在确认真实变化后于后台增量刷新。
- 普通进入页面时，绝不能引入全库扫描、逐漫画遍历目录、打开压缩包或重新生成封面。昂贵的核对应只用于用户主动刷新、已确认的目录变化、迁移或修复。
- Android SAF 或文档提供器可能暂时返回“可以访问但内容为空”。已有非空索引时，这种意外空结果不可靠：不得持久化为空、删除记录、清空计数或替换最后一次正常界面；应在不阻塞显示的前提下重试或使用已有元数据恢复。
- 保持一个长期存活的 `LocalSource` 实例和一份权威的列表/筛选状态。分页数据、工具栏计数、刷新状态、随机选择和筛选按钮都应来源于同一个快照。
- “全部”“在读”“剩余”“读完”、标记和搜索的计数必须与实际可见列表采用完全相同的筛选语义。普通筛选切换应在内存中近乎即时完成，不触发文件系统或全库数据库重载。
- 新增或删除篇目文件时，不应突然出现刷新转圈。优先采用“先显示旧数据、后台轻量核对”的方式，仅在篇目文件集合确实变化时更新。
- 文件移动后应尽可能保留稳定的篇目和数据库身份，使阅读进度、已读状态、中文译名显示数据和自定义封面继续绑定。
- 本地目录索引、篇目元数据索引、页数缓存和自动封面缓存只能保存可从磁盘重建的信息，不得成为篇目身份或用户数据的事实来源。阅读进度、阅读历史、已读、书签、译名、标记和自定义篇目封面必须以稳定的篇目数据库 ID 为归属；移动或重复行合并时要保留旧 ID，并在同一事务中迁移所有引用该 ID 的持久数据。

## 交互偏好

- 日常行为应安静、自然且符合预期。除非是用户主动触发的操作反馈，或为了避免实质性数据损失，否则不要增加自动刷新提示、阻塞式加载页、成功提示、确认弹窗或额外控件。
- 复用已有菜单、底部栏、弹窗和交互模式。相关命令应放在一起，不要分散成新的顶级按钮或一次性界面分支。
- 描述同一份筛选列表的控件和计数必须同步更新，避免卡片先变化、文字却要等待无关后台任务才追上的情况。
- 原始来源名和文件名始终是权威数据。本地篇目中文译名只是由手动编辑或外部导入导出填充的显示壳；没有译名时回退显示原名。
- 除非用户重新明确提出，否则不要加入内置 AI 翻译。

## 验证与交付

- 任何跨模块改动在实现前和交付前都必须做影响面审计：从数据生产者、稳定身份、数据库事务、缓存与索引、仓库和状态流、分页与筛选、备份恢复、文件新增/删除/移动，到封面、阅读器及所有相关界面入口逐层追踪。不能因为当前页面正常或定向测试通过，就忽略共享调用方和相邻功能。
- 新增缓存、重试、阈值或异常保护时，必须同时验证它不会阻断正常的新增、删除、批量移动、目录重命名、冷启动、缓存恢复和大库操作。保护逻辑不能用固定数量上限永久遮蔽真实磁盘变化；性能和正确性冲突时，应采用增量处理、有限并发或精确核验。
- 修改用户数据归属或移动合并逻辑时，要把阅读进度、历史、已读、书签、译名、标记、自定义封面和备份恢复视为一个整体。数据库与文件操作必须采用事务化或可补偿顺序，不能让其中一项先被删除，而其他步骤失败后无法恢复。

- 对用户可见的本地库修复，应按需要验证缓存启动、冷启动、重新进入、切换筛选、新增、删除以及文件提供器短暂空读等场景。
- 当前主设备的本地漫画目录中有 3 个已知损坏且在未 Root 状态下无法删除的文件。现场统计、目录变化判断、移动匹配和验收对数时必须明确排除这些已知异常项；不得尝试删除、修复或把它们误报为本次功能回归。直读文件系统时应使用严格 UTF-8 解码，只跳过这 3 个无法合法解码的条目；它们不得计入漫画数、篇目数、目录签名或验收差异。若尚未取得其准确路径，只能记录为待确认排除项，不能凭文件大小、扩展名或其他读取失败擅自猜测并忽略其他文件。
- 不能只凭编译或单元测试就宣称设备上的问题已经修复；条件允许时，应在已连接设备上验证实际页面状态和相关联动。
- 未经用户明确同意，不得把清除应用数据、撤销存储权限、重建整个本地库、删除索引或重新导入文件当作修复捷径。
- 诊断时允许临时开启调试访问，但正式 Vienna 交付和源码中绝不能保留 `isDebuggable = true`。完成检查后要删除临时插桩和诊断分支。
- 必要时可以安装诊断测试包，但必须明确说明它不是正式交付。只有用户明确要求出包或安装时，才构建并安装最终 APK。
- 除非用户明确要求，否则不要提交或推送。保留工作区中无关的既有改动，不要擅自回退用户修改。
